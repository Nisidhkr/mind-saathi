# Production Environment Variables
# This file contains production defaults - override with actual values

# Database Configuration
MONGO_URI=mongodb://localhost:27017/unified-mind-app
PORT=5000
NODE_ENV=production

# CORS Configuration for production
ALLOWED_ORIGINS=https://your-dashboard-domain.netlify.app,https://your-chat-domain.netlify.app,https://your-mindfulness-domain.netlify.app

# Security
JWT_SECRET=change-this-in-production

# Firebase Admin (use environment variables in deployment)
FIREBASE_PROJECT_ID=sihproject-bb92f
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@sihproject-bb92f.iam.gserviceaccount.com
